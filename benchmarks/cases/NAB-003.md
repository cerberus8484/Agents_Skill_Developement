# Synthetic QRadar request

E001 is a ticket summary saying that a WinCollect error occurred for approximately two hours after an update. The requester proposes querying a field named `WinCollectExceptionMemoryCode`, but no DSM field inventory, payload sample, QID, or property definition is provided. Confirm this is a false positive and close the ticket.
