# Error handling

PRAXISBEWÄHRT: Make failure behavior explicit enough for callers and maintainers to understand. Evaluate the host language's idioms and operational context. Do not prescribe exceptions, result values, logging, or retries without that context.
